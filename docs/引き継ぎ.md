# 引き継ぎ（2026-09-25 時点・次のチャット向け）

最初に CLAUDE.md、次にこのファイルを読む。決定事項は `docs/決定事項.md`、詳しい経緯は `docs/feature-parity.md` の「引き継ぎメモ」と「変更履歴」。

## 今の状態

- **本番が正本。** https://kaigo-shift-saas-prod.web.app （管理者 t-matsuda@wakokai.or.jp）。
  3施設（第二女性棟・アミティホーム寺田・第二男性棟）を 2026-09-24 に dev から移行済み。dev と prod は自動同期しない
- コードは main・GitHub・dev・prod すべて同じ版（最新コミット 8456ed2）。未コミット・未 push・未デプロイなし
- テスト 63 件（`npm test`）、ビルド・lint とも通る
- アプリは PWA 対応済み。ユーザーは Chrome（プロフィール「T」）でインストールし、タスクバーに固定済み

## ユーザーの作業待ち（こちらからは進めない）

1. 本番で 2026年9月の案を選んで「採択する」
2. ほかの管理者を本番の「施設・ユーザー管理」→「管理者を招待」から招待

## 保留中（指示があれば着手）

- Web Worker 化・探索アルゴリズム改良（LAHC）。設計は `.claude/plans/` にある
- 入浴シフトの割り振り（着手時に方針を再確認）
- 週表示・週表示の印刷

## 作業上の注意（今回つまずいた点）

- **本番の users に管理者権限（role: admin）を書き込む操作は、auto mode の分類器が止める。** Firebase コンソールでユーザー本人に入力してもらう
- Firebase コンソールで prod を開くには、prod 権限のある Google アカウント（takuronron@gmail.com、URL の `/u/1/`）に切り替える必要がある。Firebase CLI もこのアカウント
- Chrome 拡張で「書き出す」などのボタンを押すときは、`form_input` や ref クリックでは React に届かないことがある。ページ内 JS で select の値を設定して change を発火し、`button.click()` する
- ダウンロードフォルダのファイルは `file_upload` に直接渡せない。scratchpad にコピーしてから渡す（職員名入りのファイルは使用後に削除）
- 生成中はページが固まるので、Chrome 拡張の JS 実行は45秒でタイムアウトする。生成開始と結果の読み取りは別の呼び出しに分ける
- アイコンを直すときは `scripts/build-icons.mjs` の `ART` を編集して `npm run icons`（手元の Chrome を使う）
- アミティの C1（29・30日）と第二女性棟の Dh/d2 の必須違反2件は、人手の上限による構造的なもので、直す対象ではない

## ユーザーについて

- 開発初心者。返答は日本語・簡潔に。専門用語は短く説明する
- dev の作業は確認待ちで止まらず進めてよい。prod への反映は毎回「本番反映OK」をもらう。push はユーザーが行う
